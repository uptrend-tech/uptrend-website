/** @jsx jsx */
import { jsx } from 'theme-ui'
import { Styled, Footer as ThemeUIFooter } from 'theme-ui'

const Footer = props => (
  <ThemeUIFooter>
    <Styled.h2 sx={{ variant: 'text.h2small' }}>Where To Find Us</Styled.h2>
  </ThemeUIFooter>
)

export default Footer
